from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Use MLFlow to record models made to predict/recognize the ASL letter in given pictures(jpg)',
    author='Jayden Dayno',
    license='',
)
